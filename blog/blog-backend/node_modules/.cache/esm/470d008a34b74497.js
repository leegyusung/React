let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_34b‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_34b‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_34b‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_34b‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_34b‍.w("./api",[["default",["api"],function(v){api=v}]]);_34b‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








const { PORT, MONGO_URI } = process.env;


mongoose
    .connect(MONGO_URI, { useNewUrlParser: true, useFindAndModify: false })
    .then(() => {
        console.log("Connected to MongoDB");
    })
    .catch(e => {
        _34b‍.g.console.log(e)
    })

const app = new Koa();
const router = new Router();

app.use(bodyParser())
app.use(jwtMiddleware);

// 라우터 설정
router.use('/api', api.routes()); // api 라우트 적용

//app인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
    console.log(`${port} 포트 사용중...`)
})