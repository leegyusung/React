let jwt;_2e4‍.x([["default",()=>_2e4‍.o]]);_2e4‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
    const token = ctx.cookies.get('access_token');
    if (!token) return next();
    try {
        const decoded = jwt.verify(token, process.env.JWT_SECRET);
        ctx.state.user = {
            _id: decoded._id,
            username: decoded.username,
        }
        _2e4‍.g.console.log(decoded);
        return next();
    } catch (error) {
        return next();
    }
}

_2e4‍.d(jwtMiddleware);