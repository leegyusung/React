let jwt;_1b6‍.x([["default",()=>_1b6‍.o]]);_1b6‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);

const jwtMiddleware = (ctx, next) => {
    const token = ctx.cookies.get('access_token');
    if (!token) return next();
    try {
        const decoded = jwt.verify(token, process.env.JWT_SECRET);
        ctx.state.user = {
            _id: decoded._id,
            username: decoded.username,
        }
        _1b6‍.g.console.log(ctx.state.user);
        return next();
    } catch (error) {
        return next();
    }
}

_1b6‍.d(jwtMiddleware);